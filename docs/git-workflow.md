# Git 工作流

## 分支建议

- `main`：稳定分支。
- `feature/*`：功能开发分支。

## 提交建议

提交应按功能拆分，避免一次性提交大量无关改动。

## 忽略规则

`.gitignore` 已排除：

- `node_modules/`
- `dist/`
- `target/`
- 日志文件
- 本地环境文件
- IDE 配置

## 大文件策略

资源目录可能包含图片、构建产物。建议：

- 只提交必要模板资源。
- 导出产物不提交。
- 如必须管理大资源，可引入 Git LFS。
